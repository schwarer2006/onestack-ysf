# ---
# name: source_initial
# type: source
# description: Initial-Load der Datenquelle (z. B. CSV)
# ---

load_type: initial

source:
  format: csv
  path: /data/raw/<your_file>.csv
  delimiter: ","
  header: true
  encoding: utf-8

columns:
  - name: id
    type: string
  - name: name
    type: string
