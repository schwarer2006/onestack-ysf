# ---
# name: surrogate_key
# type: extension
# version: 1.1.0
# description: Fügt Surrogate Key zu einer beliebigen Dimension hinzu
# ---

surrogate_key:
  name: <name>_sid
  type: integer
  start: 1000
  strategy: auto_increment
  persisted: true

columns:
  - name: <name>_sid
    type: integer
    role: surrogate_key
    generated: true
    nullable: false
